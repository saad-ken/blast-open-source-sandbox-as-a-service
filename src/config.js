const defaultConfig = {
  apiUrl: '/api', // Placeholder for the backend API URL
  sandboxConfig: {
    maxScriptExecutionTime: 5000, // Maximum time (in milliseconds) a script can run in the sandbox
  },
};

module.exports = { defaultConfig };
